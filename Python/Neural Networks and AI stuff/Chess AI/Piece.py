class Piece:
        def __init__(self, name, color, symbol, rank, file) -> None:
                self.name = name
                self.color = color
                self.symbol = symbol
                self.rank = rank
                self.file = file